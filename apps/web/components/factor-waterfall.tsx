export function FactorWaterfall({ factors }: { factors: { label: string; impact: number }[] }) { return <div className="space-y-3">{factors.map((factor) => <div className="grid grid-cols-[120px_1fr_38px] items-center gap-3 text-xs" key={factor.label}><span className="truncate text-[#b8d3cf]">{factor.label}</span><div className="h-2 rounded-full bg-black/20"><div className={`h-2 rounded-full ${factor.impact >= 0 ? "bg-[#61e6cf]" : "bg-[#f6ad65]"}`} style={{ width: `${Math.min(Math.abs(factor.impact) * 5, 100)}%` }}/></div><span className={factor.impact >= 0 ? "text-[#61e6cf]" : "text-[#f6ad65]"}>{factor.impact > 0 ? "+" : ""}{factor.impact}</span></div>)}</div>; }
